---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { imageMetadata } from '@/data/imageMetadata';

const asset = (number: number, alt = '') => {
  const src = `/04_SideB/${String(number).padStart(2, '0')}_SideB.webp`;
  return { src, alt, ...imageMetadata[src] };
};

const stripImages = [
  { ...asset(1), widthCss: '31.1vw', ratio: '484 / 580' },
  { ...asset(2), widthCss: '38.2vw', ratio: '595 / 497' },
  { ...asset(3), widthCss: '26vw', ratio: '405 / 623' },
  { ...asset(4), widthCss: '25.4vw', ratio: '395 / 555' },
  { ...asset(5), widthCss: '37.1vw', ratio: '578 / 439' },
  { ...asset(6), widthCss: '27.9vw', ratio: '434 / 463' },
  { ...asset(7), widthCss: '27.1vw', ratio: '422 / 494' },
  { ...asset(8), widthCss: '36.6vw', ratio: '569 / 680' },
].map((item) => ({
  ...item,
  style: `--strip-width:${item.widthCss};--strip-ratio:${item.ratio}`,
}));

const filmImages = [
  asset(9, 'A subway passenger reflected in a black-and-white train window.'),
  asset(10, 'A close-up of bright green leaves.'),
  asset(11, 'A profile portrait in red sunglasses against a blue sky.'),
  asset(12, 'People reclining together on an outdoor chaise.'),
  asset(13, 'A black-and-white cat sitting behind a shop window.'),
  asset(14, 'A portrait in a cap with a cat paw resting across the face.'),
  asset(15, 'A black-and-white street vendor beneath an umbrella.'),
  asset(16, 'A person in a rainbow shirt waving a colorful fan.'),
  asset(17, 'A pizza maker tossing dough in the air.'),
  asset(18, 'A black-and-white portrait of an ice-cream truck driver.'),
  asset(19, 'A woman sitting beside a pool, seen through the foreground.'),
  asset(20, 'A black cat peeking above a dark window ledge.'),
];

const filmLeft = filmImages.filter((_, index) => index % 2 === 0);
const filmRight = filmImages.filter((_, index) => index % 2 === 1);
const fashion = {
  src: '/04_SideB/Fashion_SideB.webp',
  alt: 'A fashion lookbook collage of four outfits against a blue background.',
  ...imageMetadata['/04_SideB/Fashion_SideB.webp'],
};
---

<BaseLayout
  title="Side B: Personal Work — Itsmicki"
  description="The personal side of Micaela Galimberti: fashion, music, film photography and the visual references that shape her work."
  active="sideb"
  hideFooter
  customCursor
  themeColor="#fafafa"
  preconnectOrigins={['https://open.spotify.com']}
  preloadFonts={[
    '/fonts/OTNeueMontreal-MediumSemiSqueezed.otf',
    '/fonts/OTNeueMontreal-MediumExtraSqueezed.otf',
  ]}
>
  <div class="sideb-page">
    <aside class="sideb-mobile-meta" aria-label="Contact and social links">
      <span>SAY HI</span>
      <a class="sideb-mobile-mail" href="mailto:hey@itsmicki.com">HEY@ITSMICKI.COM</a>
      <span>SOCIALS</span>
      <span class="sideb-mobile-socials">
        <a href="https://www.linkedin.com/in/micaelagalimberti/" target="_blank" rel="noreferrer">LINKEDIN</a>,
        <a href="https://www.behance.net/itsmicki" target="_blank" rel="noreferrer">BEHANCE</a>,
        <a href="https://dribbble.com/micaelagalimberti" target="_blank" rel="noreferrer">DRIBBBLE</a>,
        <a href="https://www.instagram.com/its.micki/" target="_blank" rel="noreferrer">INSTAGRAM</a>,
        <span>TIKTOK</span>
      </span>
    </aside>

    <article class="sideb-content" aria-labelledby="sideb-title">
      <h1 class="sideb-title" id="sideb-title">(Some bits of irrelevant<br />sh!t I’m into)</h1>

      <div class="sideb-main">
        <section class="sideb-intro-block" aria-label="About Side B">
          <div class="sideb-intro-grid">
            <div>
              <h2 class="sideb-label">Related Links</h2>
              <p class="sideb-big sideb-related" aria-label="Related profiles">
                <span class="sideb-pseudo-link">Letterboxd</span><br />
                <span class="sideb-pseudo-link">Lomography</span><br />
                <span class="sideb-pseudo-link">Indyx</span><br />
                <span class="sideb-pseudo-link">Substack</span>
              </p>
            </div>
            <div>
              <h2 class="sideb-label">In a Nutshell</h2>
              <p class="sideb-big">Hey — On my B-side, I’m still micki, just a little more laid-back and messy. I study fashion design, take photos, make endless playlists, sell vintage and secondhand clothes, and can spend hours scrolling or watching the most cursed YouTube videos imaginable.</p>
            </div>
          </div>

          <div class="sideb-strip" data-marquee aria-label="A moving collage of Micaela's visual references">
            <div class="sideb-strip-track" data-marquee-track>
              {[false, true].map((duplicate) => (
                <div class="sideb-strip-set" data-marquee-set={duplicate ? undefined : ''} aria-hidden={duplicate ? 'true' : undefined}>
                  {stripImages.map((item) => (
                    <figure class="sideb-strip-item" style={item.style}>
                      <img
                        src={item.src}
                        alt=""
                        width={item.width}
                        height={item.height}
                        loading="lazy"
                        decoding="async"
                        draggable="false"
                      />
                    </figure>
                  ))}
                </div>
              ))}
            </div>
          </div>
        </section>

        <section class="sideb-section" aria-labelledby="fashion-title">
          <h2 class="sideb-section-title" id="fashion-title">(fashion &amp;<br />aesthetics)</h2>
          <div class="sideb-section-column">
            <p class="sideb-big">I run a <span class="sideb-pseudo-link">digital vintage shop</span> centered on curating designer pieces and extending their life through reuse. I'm fascinated by the way objects can be recontextualized, transformed, and given new meaning over time in a world where trends are fleeting.</p>

            <div class="sideb-fashion-reveal" data-sideb-reveal>
              <figure class="sideb-fashion-media">
                <img
                  data-parallax
                  src={fashion.src}
                  alt={fashion.alt}
                  width={fashion.width}
                  height={fashion.height}
                  loading="lazy"
                  decoding="async"
                />
              </figure>
            </div>

            <div class="sideb-body-pair">
              <p>I'm currently pursuing a Bachelor's degree in Design and Aesthetic Management, a multidisciplinary program that explores fashion, graphic, interior, and product design. What fascinates me most is the idea that aesthetics are never superficial. Every visual decision, from the spaces we inhabit to the clothes we wear, communicates identity, values, and culture.</p>
              <p>I see the way I dress as an extension of my design practice. It's one of the most immediate ways I present myself to the world, just as branding, art direction, or digital experiences shape how people perceive a brand. Studying aesthetics across different disciplines has expanded the way I think about communication, helping me design with greater intention, coherence, and emotional depth.</p>
            </div>
          </div>
        </section>

        <section class="sideb-section" aria-labelledby="music-title">
          <h2 class="sideb-section-title" id="music-title">(music)</h2>
          <div class="sideb-section-column">
            <p class="sideb-big">Music taught me that complexity doesn't have to feel chaotic. I'm fascinated by sound, noise, and the absence of both. Through music, I found the same eclectic spirit that defines my work across design, fashion, and aesthetics: different influences, multiple layers, and unexpected contrasts coming together to create something cohesive. My next creative goal is to dive deeper into sound design and music production, expanding the way I think about storytelling beyond the visual.</p>
          </div>
        </section>

        <section class="sideb-spotify" data-sideb-reveal aria-label="Micaela's Spotify playlist">
          <div class="sideb-spotify-frame">
            <iframe
              title="Micaela's Spotify playlist"
              src="https://open.spotify.com/embed/playlist/4WzNfZg1nlNPVuNooWu7FZ?utm_source=generator&theme=0&si=da993964294346ec"
              loading="lazy"
              allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture"
              referrerpolicy="strict-origin-when-cross-origin"
            ></iframe>
          </div>
          <a href="https://open.spotify.com/playlist/4WzNfZg1nlNPVuNooWu7FZ" target="_blank" rel="noreferrer">Abrir la playlist en Spotify ↗</a>
        </section>

        <section class="sideb-section sideb-film-section" aria-labelledby="film-title">
          <h2 class="sideb-section-title" id="film-title">(film)</h2>
          <div class="sideb-section-column">
            <p class="sideb-big">Shooting on film completely changed the way I approach uncertainty. I fell in love with its grain, textures, light leaks, and chemical imperfections, elements that digital workflows often try to eliminate.</p>

            <div
              class="sideb-film-pair"
              data-film-carousel
              role="region"
              aria-roledescription="carousel"
              aria-label="Film photography pairs"
              tabindex="0"
            >
              <div class="sideb-film-slot sideb-film-slot-left">
                <div class="sideb-film-track" data-film-track>
                  {filmLeft.map((item, index) => (
                    <figure class="sideb-film-cell" aria-hidden={index === 0 ? 'false' : 'true'}>
                      <img data-parallax src={item.src} alt={item.alt} width={item.width} height={item.height} loading="lazy" decoding="async" draggable="false" />
                    </figure>
                  ))}
                </div>
              </div>
              <div class="sideb-film-slot sideb-film-slot-right">
                <div class="sideb-film-track" data-film-track>
                  {filmRight.map((item, index) => (
                    <figure class="sideb-film-cell" aria-hidden={index === 0 ? 'false' : 'true'}>
                      <img data-parallax src={item.src} alt={item.alt} width={item.width} height={item.height} loading="lazy" decoding="async" draggable="false" />
                    </figure>
                  ))}
                </div>
              </div>
              <p class="sideb-sr-only" data-film-status aria-live="polite">Film pair 1 of 6</p>
            </div>

            <div class="sideb-body-pair">
              <p>What fascinates me most is capturing moments that might seem ordinary or even unnoticed to everyone else, but somehow feel worth preserving to me. Photography gives me the chance to steal those fleeting moments and reveal the beauty hidden in what others might overlook.</p>
              <p>Every frame demands patience, intention, and the acceptance that the outcome is never fully under your control. That process has deeply influenced the way I approach design: slowing down, trusting the process, and finding beauty in what wasn't planned. I learned that uncertainty isn't the opposite of good design. Sometimes it's what gives it character.</p>
            </div>
          </div>
        </section>
      </div>
    </article>

    <footer class="sideb-footer" id="sideb-footer">
      <div class="sideb-footer-wordmark" aria-label="Itsmicki, copyright 2026">
        <span data-sideb-reveal>Itsmicki</span>
        <span data-sideb-reveal>©2026</span>
      </div>
      <div class="sideb-footer-info" id="sideb-footer-info">
        <span data-sideb-reveal style="--reveal-delay:0ms">DEV BY</span>
        <a data-sideb-reveal style="--reveal-delay:70ms" href="https://www.instagram.com/luckystoned/" target="_blank" rel="noreferrer">@LUCKYSTONED</a>
        <span data-sideb-reveal style="--reveal-delay:140ms">SAY HI</span>
        <a data-sideb-reveal style="--reveal-delay:210ms" href="mailto:hey@itsmicki.com">HEY@ITSMICKI.COM</a>
        <span data-sideb-reveal style="--reveal-delay:280ms">ALL RIGHTS RESERVED.</span>
        <span class="sideb-footer-socials" data-sideb-reveal style="--reveal-delay:350ms">
          SOCIALS <a href="https://www.linkedin.com/in/micaelagalimberti/" target="_blank" rel="noreferrer">LINKEDIN</a>,
          <a href="https://www.behance.net/itsmicki" target="_blank" rel="noreferrer">BEHANCE</a>,
          <a href="https://dribbble.com/micaelagalimberti" target="_blank" rel="noreferrer">DRIBBBLE</a>,
          <a href="https://www.instagram.com/its.micki/" target="_blank" rel="noreferrer">INSTAGRAM</a>,
          <span>TIKTOK</span>
        </span>
      </div>
    </footer>
  </div>

  <script>
    /* Native scrolling with the same damped wheel interpolation as the reference. */
    (function () {
      const reduceMotion = matchMedia('(prefers-reduced-motion:reduce)').matches;
      if (reduceMotion || !matchMedia('(pointer:fine)').matches) return;

      let targetY = window.scrollY;
      let currentY = targetY;
      let running = false;
      let frame: number | null = null;
      let locked = false;
      let previousTime = 0;

      const maxScroll = () => Math.max(0, document.documentElement.scrollHeight - window.innerHeight);

      function tick(now: number) {
        if (!previousTime) previousTime = now;
        const deltaTime = Math.min(64, now - previousTime) / 1000;
        previousTime = now;
        currentY += (targetY - currentY) * (1 - Math.exp(-6.5 * deltaTime));
        if (Math.abs(targetY - currentY) < 0.12) {
          currentY = targetY;
          running = false;
          previousTime = 0;
        }
        locked = true;
        window.scrollTo(0, currentY);
        locked = false;
        frame = running ? requestAnimationFrame(tick) : null;
      }

      addEventListener('wheel', (event) => {
        if (event.ctrlKey) return;
        event.preventDefault();
        let delta = event.deltaY;
        if (event.deltaMode === 1) delta *= 16;
        else if (event.deltaMode === 2) delta *= window.innerHeight;
        targetY = Math.max(0, Math.min(maxScroll(), targetY + delta));
        if (!running) {
          running = true;
          if (!frame) frame = requestAnimationFrame(tick);
        }
      }, { passive: false });

      addEventListener('scroll', () => {
        if (!locked && !running) targetY = currentY = window.scrollY;
      });

      addEventListener('resize', () => {
        targetY = currentY = window.scrollY;
      });

      document.querySelectorAll<HTMLAnchorElement>('a[href^="#"]').forEach((anchor) => {
        anchor.addEventListener('click', (event) => {
          const href = anchor.getAttribute('href');
          if (!href || href === '#') return;
          const target = document.querySelector<HTMLElement>(href);
          if (!target) return;
          event.preventDefault();
          targetY = Math.max(0, Math.min(maxScroll(), target.getBoundingClientRect().top + window.scrollY));
          if (!running) {
            running = true;
            if (!frame) frame = requestAnimationFrame(tick);
          }
        });
      });
    })();

    const reduceSideBMotion = matchMedia('(prefers-reduced-motion: reduce)').matches;

    document.querySelectorAll<HTMLElement>('[data-sideb-reveal]').forEach((node) => {
      if (reduceSideBMotion) node.classList.add('is-visible');
    });

    if (!reduceSideBMotion) {
      const revealObserver = new IntersectionObserver((entries) => {
        entries.forEach((entry) => entry.target.classList.toggle('is-visible', entry.isIntersecting));
      }, { threshold: .02, rootMargin: '-2% 0px -6% 0px' });
      document.querySelectorAll<HTMLElement>('[data-sideb-reveal]').forEach((node) => revealObserver.observe(node));
    }

    const marquee = document.querySelector<HTMLElement>('[data-marquee]');
    const marqueeTrack = marquee?.querySelector<HTMLElement>('[data-marquee-track]');
    const marqueeSet = marquee?.querySelector<HTMLElement>('[data-marquee-set]');

    if (marquee && marqueeTrack && marqueeSet) {
      let setWidth = 0;
      let offset = 0;
      let lastTime = performance.now();
      let hovering = false;
      let dragging = false;
      let pointerId = -1;
      let pointerStart = 0;
      let offsetStart = 0;
      let resizeFrame = 0;

      const normalizeOffset = () => {
        if (!setWidth) return;
        while (offset <= -setWidth * 2) offset += setWidth;
        while (offset > -setWidth) offset -= setWidth;
      };

      const renderMarquee = () => {
        marqueeTrack.style.transform = `translate3d(${offset}px, 0, 0)`;
      };

      const measureMarquee = () => {
        const gap = parseFloat(getComputedStyle(marqueeTrack).columnGap || getComputedStyle(marqueeTrack).gap) || 0;
        setWidth = marqueeSet.getBoundingClientRect().width + gap;
        offset = -setWidth;
        renderMarquee();
      };

      const animateMarquee = (now: number) => {
        const deltaTime = Math.min(64, now - lastTime);
        lastTime = now;
        if (!reduceSideBMotion && !dragging) {
          offset -= deltaTime * (hovering ? .006 : .03);
          normalizeOffset();
          renderMarquee();
        }
        requestAnimationFrame(animateMarquee);
      };

      marquee.addEventListener('mouseenter', () => { hovering = true; });
      marquee.addEventListener('mouseleave', () => { hovering = false; });
      marquee.addEventListener('pointerdown', (event) => {
        dragging = true;
        pointerId = event.pointerId;
        pointerStart = event.clientX;
        offsetStart = offset;
        marquee.setPointerCapture(pointerId);
        marquee.classList.add('is-dragging');
      });
      marquee.addEventListener('pointermove', (event) => {
        if (!dragging || event.pointerId !== pointerId) return;
        offset = offsetStart + event.clientX - pointerStart;
        normalizeOffset();
        renderMarquee();
      });
      const finishDrag = (event: PointerEvent) => {
        if (!dragging || event.pointerId !== pointerId) return;
        dragging = false;
        marquee.classList.remove('is-dragging');
        if (marquee.hasPointerCapture(pointerId)) marquee.releasePointerCapture(pointerId);
        pointerId = -1;
      };
      marquee.addEventListener('pointerup', finishDrag);
      marquee.addEventListener('pointercancel', finishDrag);
      addEventListener('resize', () => {
        cancelAnimationFrame(resizeFrame);
        resizeFrame = requestAnimationFrame(measureMarquee);
      });

      if (document.fonts?.ready) document.fonts.ready.then(measureMarquee);
      else measureMarquee();
      if (!reduceSideBMotion) requestAnimationFrame(animateMarquee);
    }

    const filmCarousel = document.querySelector<HTMLElement>('[data-film-carousel]');
    if (filmCarousel) {
      const tracks = Array.from(filmCarousel.querySelectorAll<HTMLElement>('[data-film-track]'));
      const status = filmCarousel.querySelector<HTMLElement>('[data-film-status]');
      const count = tracks[0]?.children.length ?? 0;
      let index = 0;
      let timer = 0;
      let pointerId = -1;
      let pointerStart = 0;

      const renderFilm = () => {
        tracks.forEach((track) => {
          track.style.transform = `translate3d(${-index * 100}%, 0, 0)`;
          Array.from(track.children).forEach((cell, cellIndex) => {
            cell.setAttribute('aria-hidden', cellIndex === index ? 'false' : 'true');
          });
        });
        if (status) status.textContent = `Film pair ${index + 1} of ${count}`;
      };

      const goTo = (nextIndex: number) => {
        if (!count) return;
        index = (nextIndex + count) % count;
        renderFilm();
      };

      const stopFilm = () => {
        if (timer) window.clearInterval(timer);
        timer = 0;
      };

      const startFilm = () => {
        stopFilm();
        if (!reduceSideBMotion && count > 1) timer = window.setInterval(() => goTo(index + 1), 3800);
      };

      filmCarousel.addEventListener('mouseenter', stopFilm);
      filmCarousel.addEventListener('mouseleave', startFilm);
      filmCarousel.addEventListener('focusin', stopFilm);
      filmCarousel.addEventListener('focusout', startFilm);
      filmCarousel.addEventListener('keydown', (event) => {
        if (event.key !== 'ArrowLeft' && event.key !== 'ArrowRight') return;
        event.preventDefault();
        goTo(index + (event.key === 'ArrowRight' ? 1 : -1));
      });
      filmCarousel.addEventListener('pointerdown', (event) => {
        pointerId = event.pointerId;
        pointerStart = event.clientX;
        filmCarousel.setPointerCapture(pointerId);
        stopFilm();
      });
      const finishFilmSwipe = (event: PointerEvent) => {
        if (event.pointerId !== pointerId) return;
        const distance = event.clientX - pointerStart;
        if (Math.abs(distance) >= 34) goTo(index + (distance < 0 ? 1 : -1));
        if (filmCarousel.hasPointerCapture(pointerId)) filmCarousel.releasePointerCapture(pointerId);
        pointerId = -1;
        startFilm();
      };
      filmCarousel.addEventListener('pointerup', finishFilmSwipe);
      filmCarousel.addEventListener('pointercancel', finishFilmSwipe);
      renderFilm();
      startFilm();
    }

    if (!reduceSideBMotion) {
      const parallaxItems = Array.from(document.querySelectorAll<HTMLElement>('[data-parallax]'));
      let parallaxFrame = 0;

      const updateParallax = () => {
        parallaxFrame = 0;
        parallaxItems.forEach((item) => {
          const frame = item.parentElement;
          if (!frame) return;
          const rect = frame.getBoundingClientRect();
          if (rect.bottom < -300 || rect.top > innerHeight + 300) return;
          const progress = (rect.top + rect.height / 2 - innerHeight / 2) / (innerHeight + rect.height);
          item.style.transform = `translate3d(0, ${progress * rect.height * -.14}px, 0)`;
        });
      };

      const requestParallax = () => {
        if (!parallaxFrame) parallaxFrame = requestAnimationFrame(updateParallax);
      };

      addEventListener('scroll', requestParallax, { passive: true });
      addEventListener('resize', requestParallax);
      requestParallax();
    }
  </script>
</BaseLayout>

<style is:global>
  @font-face {
    font-family: 'OT Neue Montreal SideB';
    src: url('/fonts/OTNeueMontreal-MediumSemiSqueezed.otf') format('opentype');
    font-style: normal;
    font-weight: 400 600;
    font-display: swap;
  }

  @font-face {
    font-family: 'OT Neue Montreal SideB Extra';
    src: url('/fonts/OTNeueMontreal-MediumExtraSqueezed.otf') format('opentype');
    font-style: normal;
    font-weight: 400 600;
    font-display: swap;
  }

  html:has(.sideb-page),
  body:has(.sideb-page) {
    --sideb-ink: #2e2e2e;
    --sideb-paper: #fafafa;
    --sideb-pad: clamp(14px, 1.42vw, 22px);
    --sideb-ease: cubic-bezier(.19, 1, .22, 1);
    background: var(--sideb-paper);
    color: var(--sideb-ink);
    cursor: url('/cursors/view.png') 0 0, auto;
    -webkit-font-smoothing: antialiased;
    overflow-x: hidden;
  }

  html:has(.sideb-page) {
    scroll-behavior: auto;
  }

  body:has(.sideb-page) a,
  body:has(.sideb-page) [data-marquee],
  body:has(.sideb-page) [data-film-carousel] {
    cursor: url('/cursors/view.png') 0 0, auto;
    -webkit-tap-highlight-color: transparent;
  }

  body:has(.sideb-page) a:hover,
  body:has(.sideb-page) a:focus-visible,
  body:has(.sideb-page) [data-marquee]:active {
    cursor: url('/cursors/view-hover.png') 0 0, pointer;
  }

  body:has(.sideb-page) ::selection {
    background: var(--sideb-ink);
    color: var(--sideb-paper);
  }

  body:has(.sideb-page) .site-header {
    position: relative;
    z-index: 20;
    top: auto;
    padding: 24px var(--sideb-pad) 0;
    background: transparent;
    color: var(--sideb-ink);
    backdrop-filter: none;
    font-family: 'OT Neue Montreal SideB', 'Arial Narrow', sans-serif;
    font-size: clamp(14px, 1.74vw, 27.117px);
    font-weight: 500;
    line-height: normal;
    letter-spacing: -.02em;
  }

  body:has(.sideb-page) .site-header nav {
    gap: 10px;
  }

  body:has(.sideb-page) .site-header a::after {
    display: none;
  }

  body:has(.sideb-page) .site-header a:is(:hover, :focus-visible, .active) {
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: .08em;
  }

  .sideb-page {
    min-height: 100vh;
    background: var(--sideb-paper);
    color: var(--sideb-ink);
    font-family: 'OT Neue Montreal SideB', 'Arial Narrow', sans-serif;
    font-weight: 500;
    letter-spacing: -.01em;
  }

  .sideb-mobile-meta {
    display: none;
  }

  .sideb-title {
    margin: clamp(30px, 6.6vw, 105px) 0 0;
    font-family: 'OT Neue Montreal SideB Extra', Impact, sans-serif;
    font-size: 20.77vw;
    font-weight: 500;
    line-height: .854;
    letter-spacing: -.04em;
  }

  .sideb-main {
    display: flex;
    flex-direction: column;
    gap: clamp(40px, 8.3vw, 129px);
    margin-top: clamp(40px, 9vw, 140px);
  }

  .sideb-intro-grid {
    display: grid;
    grid-template-columns: 556fr 994fr;
    gap: clamp(20px, 2vw, 32px);
  }

  .sideb-label {
    margin: 0 0 18px;
    font-size: clamp(16px, 1.54vw, 24px);
    font-weight: 500;
    line-height: 1;
  }

  .sideb-big,
  .sideb-section-title {
    margin: 0;
    font-size: clamp(30px, 4.137vw, 64.53px);
    font-weight: 500;
    line-height: .868;
    letter-spacing: -.025em;
  }

  .sideb-related {
    line-height: .91;
  }

  .sideb-pseudo-link {
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: .08em;
  }

  .sideb-strip {
    height: 40.4vw;
    margin-top: clamp(20px, 2.6vw, 40px);
    overflow: hidden;
    touch-action: pan-y;
    user-select: none;
  }

  .sideb-strip-track,
  .sideb-strip-set {
    display: flex;
    align-items: center;
    width: max-content;
    height: 100%;
    gap: 3.08vw;
  }

  .sideb-strip-track {
    will-change: transform;
  }

  .sideb-strip-item {
    flex: 0 0 var(--strip-width);
    width: var(--strip-width);
    aspect-ratio: var(--strip-ratio);
    margin: 0;
  }

  .sideb-strip-item img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
    pointer-events: none;
  }

  .sideb-strip.is-dragging {
    cursor: grabbing;
  }

  .sideb-section {
    display: grid;
    grid-template-columns: 281fr 1269fr;
    gap: clamp(14px, 1.3vw, 20px);
  }

  .sideb-section-title {
    font-family: 'OT Neue Montreal SideB', 'Arial Narrow', sans-serif;
  }

  .sideb-section-column {
    display: flex;
    flex-direction: column;
    gap: clamp(18px, 3.1vw, 48px);
    align-items: flex-end;
    min-width: 0;
  }

  .sideb-fashion-media {
    position: relative;
    width: 100%;
    aspect-ratio: 1265 / 572;
    margin: 0;
    overflow: hidden;
    clip-path: inset(0 0 100% 0);
    transition: clip-path 1s var(--sideb-ease);
  }

  .sideb-fashion-reveal {
    width: 100%;
  }

  .sideb-fashion-media img {
    position: absolute;
    top: -7%;
    left: 0;
    display: block;
    width: 100%;
    height: 114%;
    object-fit: cover;
    scale: 1.05;
    transition: scale 1.4s var(--sideb-ease);
    will-change: transform;
  }

  .sideb-fashion-reveal.is-visible .sideb-fashion-media {
    clip-path: inset(0);
  }

  .sideb-fashion-reveal.is-visible .sideb-fashion-media img {
    scale: 1;
  }

  .sideb-body-pair {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    width: 100%;
    gap: clamp(20px, 2vw, 32px);
  }

  .sideb-body-pair p {
    margin: 0;
    font-size: clamp(14.4px, 2.28vw, 35.48px);
    line-height: .965;
    letter-spacing: -.01em;
  }

  .sideb-body-pair p:first-child {
    width: 44.7%;
  }

  .sideb-body-pair p:last-child {
    width: 50.3%;
  }

  .sideb-spotify {
    opacity: 0;
    transform: translateY(16px);
    transition:
      opacity .85s var(--sideb-ease),
      transform .95s var(--sideb-ease);
  }

  .sideb-spotify.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  .sideb-spotify-frame {
    width: 100%;
    aspect-ratio: 1555.62 / 740;
  }

  .sideb-spotify iframe {
    display: block;
    width: 100%;
    height: 100%;
    border: 0;
    border-radius: 12px;
  }

  .sideb-spotify > a {
    display: inline-block;
    margin-top: 8px;
    font-size: clamp(14px, 1.31vw, 21px);
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: .08em;
  }

  .sideb-film-pair {
    display: grid;
    grid-template-columns: 711fr 540fr;
    gap: clamp(10px, 1.5vw, 23.6px);
    width: 100%;
    outline: none;
    touch-action: pan-y;
  }

  .sideb-film-pair:focus-visible {
    outline: 2px solid currentColor;
    outline-offset: 6px;
  }

  .sideb-film-slot {
    position: relative;
    overflow: hidden;
  }

  .sideb-film-slot-left {
    aspect-ratio: 711 / 676;
  }

  .sideb-film-slot-right {
    aspect-ratio: 540 / 676;
  }

  .sideb-film-track {
    display: flex;
    width: 100%;
    height: 100%;
    transition: transform .85s var(--sideb-ease);
    will-change: transform;
  }

  .sideb-film-cell {
    position: relative;
    flex: 0 0 100%;
    width: 100%;
    height: 100%;
    margin: 0;
    overflow: hidden;
  }

  .sideb-film-cell img {
    position: absolute;
    top: -7%;
    left: 0;
    display: block;
    width: 100%;
    height: 114%;
    object-fit: cover;
    pointer-events: none;
    will-change: transform;
  }

  .sideb-sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .sideb-footer {
    position: relative;
    margin-top: clamp(26px, 3.6vw, 56px);
    overflow: hidden;
  }

  .sideb-footer-wordmark {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    color: var(--sideb-ink);
    font-family: 'OT Neue Montreal SideB Extra', Impact, sans-serif;
    font-size: 37.4vw;
    font-weight: 500;
    line-height: .79;
    letter-spacing: -.04em;
    white-space: nowrap;
  }

  .sideb-footer-wordmark span:first-child { margin-left: -1.5vw; }
  .sideb-footer-wordmark span:last-child { margin-right: -1.4vw; }

  .sideb-footer-info {
    position: absolute;
    z-index: 2;
    right: 0;
    bottom: 5vw;
    left: 0;
    display: flex;
    align-items: flex-end;
    gap: 0;
    padding: 0 var(--sideb-pad);
    font-size: clamp(10px, 1.31vw, 21px);
    line-height: 1;
    letter-spacing: -.02em;
    text-transform: uppercase;
  }

  .sideb-footer-info > * {
    width: fit-content;
    padding-top: 1px;
    background: var(--sideb-ink);
    color: var(--sideb-paper);
    white-space: nowrap;
  }

  .sideb-footer-info > :nth-child(1) { margin-left: -.2%; }
  .sideb-footer-info > :nth-child(2) { margin-left: 5.1%; }
  .sideb-footer-info > :nth-child(3) { margin-left: 5.2%; }
  .sideb-footer-info > :nth-child(4) { margin-left: 5.1%; }
  .sideb-footer-info > :nth-child(5) { margin-left: 6.1%; }
  .sideb-footer-info > :nth-child(6) { margin-left: auto; }

  .sideb-footer-info a:is(:hover, :focus-visible) {
    color: var(--sideb-paper);
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: .08em;
  }

  .sideb-footer [data-sideb-reveal] {
    opacity: 0;
    transform: translateY(16px);
    transition:
      opacity .85s var(--sideb-ease) var(--reveal-delay, 0ms),
      transform .95s var(--sideb-ease) var(--reveal-delay, 0ms);
  }

  .sideb-footer [data-sideb-reveal].is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  @media (max-width: 760px) {
    body:has(.sideb-page) {
      padding-bottom: calc(58px + env(safe-area-inset-bottom));
    }

    body:has(.sideb-page) .site-header {
      position: fixed;
      z-index: 40;
      inset: auto 0 max(13px, env(safe-area-inset-bottom));
      display: flex;
      justify-content: center;
      align-items: flex-start;
      gap: 3.076923vw;
      width: 100vw;
      min-height: 0;
      padding: 0;
      background: transparent;
      font-size: 6.794359vw;
      line-height: 1;
      pointer-events: none;
    }

    body:has(.sideb-page) .site-header .wordmark,
    body:has(.sideb-page) .site-header nav {
      pointer-events: auto;
    }

    body:has(.sideb-page) .site-header nav {
      gap: 3.076923vw;
    }

    body:has(.sideb-page) .site-header a {
      white-space: nowrap;
    }

    .sideb-mobile-meta {
      position: absolute;
      z-index: 30;
      top: max(2.051282vw, env(safe-area-inset-top));
      right: env(safe-area-inset-right);
      left: env(safe-area-inset-left);
      display: grid;
      grid-template-columns: auto 1fr;
      align-items: start;
      color: var(--sideb-ink);
      font-size: 4.102564vw;
      line-height: 1;
      letter-spacing: -.02em;
      text-transform: uppercase;
    }

    .sideb-mobile-mail,
    .sideb-mobile-socials {
      justify-self: end;
      background: var(--sideb-ink);
      color: var(--sideb-paper);
      white-space: nowrap;
    }

    .sideb-mobile-mail,
    .sideb-mobile-socials a {
      color: var(--sideb-paper);
      text-decoration: underline;
      text-decoration-thickness: 1px;
      text-underline-offset: .08em;
    }

    .sideb-title {
      margin-top: 30.7vw;
      padding: 0 18px;
      font-size: 24vw;
      line-height: .92;
    }

    .sideb-main {
      gap: 40px;
      margin-top: 40px;
    }

    .sideb-intro-grid,
    .sideb-section {
      grid-template-columns: 1fr;
      gap: 16px;
      padding: 0 18px;
    }

    .sideb-intro-grid {
      gap: 16px;
    }

    .sideb-label {
      margin-bottom: 3.8vw;
      font-size: 4.7vw;
    }

    .sideb-big,
    .sideb-section-title {
      font-size: clamp(21.6px, 6.1vw, 32px);
      line-height: 1;
    }

    .sideb-related {
      line-height: .98;
    }

    .sideb-strip {
      height: 62vw;
      margin-top: 8vw;
    }

    .sideb-section-column {
      gap: 20px;
      align-items: stretch;
    }

    .sideb-body-pair {
      flex-direction: column;
      gap: 6vw;
    }

    .sideb-body-pair p,
    .sideb-body-pair p:first-child,
    .sideb-body-pair p:last-child {
      width: 100%;
      font-size: clamp(14.72px, 4.3vw, 20.8px);
      line-height: 1.16;
    }

    .sideb-spotify {
      width: calc(100% - 36px);
      margin: 0 18px;
    }

    .sideb-spotify-frame {
      height: min(600px, 134vw);
      aspect-ratio: auto;
    }

    .sideb-spotify > a {
      font-size: 4.3vw;
    }

    .sideb-film-pair {
      grid-template-columns: 1fr;
      gap: 10px;
    }

    .sideb-film-slot-left,
    .sideb-film-slot-right {
      aspect-ratio: 1080 / 1352;
    }

    .sideb-footer {
      margin-top: 14vw;
      padding-top: 8vw;
      padding-bottom: calc(18px + env(safe-area-inset-bottom));
    }

    .sideb-footer::before {
      position: absolute;
      top: 0;
      right: 18px;
      left: 18px;
      height: 1px;
      content: '';
      background: color-mix(in srgb, var(--sideb-ink) 38%, transparent);
    }

    .sideb-footer-wordmark {
      font-size: 26vw;
      line-height: .85;
    }

    .sideb-footer-wordmark span:first-child { margin-left: -1.5vw; }
    .sideb-footer-wordmark span:last-child { margin-right: -1.4vw; }
    .sideb-footer-info { display: none; }
  }

  @media (prefers-reduced-motion: reduce) {
    .sideb-page *,
    .sideb-page *::before,
    .sideb-page *::after {
      animation-duration: .01ms !important;
      animation-iteration-count: 1 !important;
      scroll-behavior: auto !important;
      transition-duration: .01ms !important;
    }

    .sideb-page [data-sideb-reveal] {
      opacity: 1 !important;
      transform: none !important;
      clip-path: none !important;
    }

    .sideb-page [data-parallax] {
      transform: none !important;
    }
  }
</style>
